{
  "require": {
    "google/cloud-firestore": "^1.0",
    "irazasyed/telegram-bot-sdk": "^3.0"
  },
  "scripts": {
    "post-install-cmd": [
      "cp vendor/google/cloud-firestore/composer.json composer.json"
    ]
  }
}
